kind = "web"
previewPath = "/"
title = "PRD → Sprint Planner"
version = "1.0.0"
id = "artifacts/prd-planner"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23870

[services.development]
run = "pnpm --filter @workspace/prd-planner run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/prd-planner", "run", "build" ]
publicDir = "artifacts/prd-planner/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23870"
BASE_PATH = "/"
